server:
  servlet:
    context-path: /ms-st

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
spring:
  data:
    mongodb:
      database: test
      port: 27017
      host: localhost

springdoc:
  api-docs:
    enabled: true

#WebSecurity
jwt:
  secret: zdtlD3JK56m6wTTgsNFhqzjqP
  issuer: br.com.arcls
